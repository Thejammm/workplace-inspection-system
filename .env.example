# ══════════════════════════════════════════════════════════════
#  Environment variables — copy to .env locally, set in Render dashboard for live
#  Never commit .env (it's gitignored). Commit .env.example only.
# ══════════════════════════════════════════════════════════════

# Port the server listens on. Render sets this automatically.
PORT=3000

# ── Phase B (auth) — will be added later ──
# SESSION_SECRET=
# ADMIN_EMAIL=
# ADMIN_PASSWORD=

# ── Phase C (database) — will be added later ──
# DATABASE_URL=
